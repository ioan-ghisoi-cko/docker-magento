version: 2

jobs:
  magento21:
    docker:
      - image: circleci/node:9.4.0
      
    steps:
      - checkout
      
  magento22:
    docker:
      - image: ./developer/Dockerfile
    steps:
      - checkout 
      # This should go into custom primary image, here's only for the sake of explanation
      - run:
          name: Start container and verify it's asd
          command: pwd

workflows:
  version: 2

  my_workflows:
    jobs:
      - magento21
      - magento22